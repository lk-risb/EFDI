allowBuilds:
  esbuild: true

# GHSA-fxqj-rqcc-2cmp / GHSA-r28c-9q8g-f849 (postcss) and GHSA-28wg-ghj8-5hjv /
# GHSA-2v37-7h3g-55p8 (nanoid) — both pulled in transitively via vite, dev-only
# and never shipped to the browser, but pinned to patched versions anyway.
#
# GHSA-c83g-rgw3-j3cx / GHSA-73wf-gq98-2v4g (browserslist) and GHSA-w5vr-8v7q-w6rv
# (baseline-browser-mapping) — pulled in transitively via @babel/core (itself via
# @tanstack/router-plugin and @vitejs/plugin-react), dev/build-time only.
overrides:
  postcss: "8.5.26"
  nanoid: "3.3.18"
  browserslist: ">=4.28.7"
  baseline-browser-mapping: ">=2.11.0"
