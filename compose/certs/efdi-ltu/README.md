# LTU fabric identity

See `docs/INSTALL.md` §2.2 "Generate certificates" for required filenames and
the `scripts/connect-ltu.sh` onboarding flow. This placeholder exists only to
keep this gitignored directory present after `git clone`.
