# Local EFDI identity

See `docs/03-bootstrap-and-install.md` "Generate certificates" for required filenames and
preparation steps. This placeholder exists only to keep this gitignored
directory present after `git clone`.
