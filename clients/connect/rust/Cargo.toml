[package]
name = "efdi_connect"
version = "0.1.0"
edition = "2021"
description = "Open an mTLS Zenoh session to an EFDI pod from env vars (the only EFDI-specific code you need)."
license = "EPL-2.0 OR Apache-2.0"

[dependencies]
# Pinned to the fleet's Zenoh 1.9.0. The 1.x API is async (tokio).
zenoh = "=1.9.0"
serde_json = "1"
